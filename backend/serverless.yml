org: willburket
app: ticket-finder
service: ticket-finder
frameworkVersion: '3'
	
plugins:
  - serverless-offline


provider:
  name: aws
  runtime: nodejs18.x

functions:
  key:
    handler: handlers/key.handler
    events:
      - httpApi:
          path: /key
          method: get

  hello:
    handler: handlers/hello.handler
    events:
      - httpApi:
          path: /hello
          method: get

